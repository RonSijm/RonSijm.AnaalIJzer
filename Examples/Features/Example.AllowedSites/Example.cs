// Example.AllowedSites is intentionally split into two files per dependency site:
//   Allowed{Site}.cs demonstrates allowedSites="{Site}".
//   Blocked{Site}.cs demonstrates blockedSites="{Site}".
//
// Building this project should produce one ARCH001 diagnostic from each file,
// for 24 diagnostics total.
